import React from "react";
import { DashboardLayout } from "@/components/layout/dashboard-layout";

export default function DashboardRoutesLayout({ children }) {
  return <DashboardLayout>{children}</DashboardLayout>;
}
